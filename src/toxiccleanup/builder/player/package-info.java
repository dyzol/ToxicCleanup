/**
 * The player package contains everything related to the player character. The
 * {@link toxiccleanup.builder.player.Cleaner} is the visual entity rendered on screen, controlled by the
 * {@link toxiccleanup.builder.player.PlayerManager} which handles keyboard input, movement, HP tracking,
 * and tile interactions. The {@link toxiccleanup.builder.player.Player} interface and
 * {@link toxiccleanup.builder.player.Harmable} interface define the contract used by other game components
 * to query and modify player state.
 */
package toxiccleanup.builder.player;
