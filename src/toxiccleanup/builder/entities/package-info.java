/**
 * Game entities are the objects that exist and interact within the game world. This package
 * contains {@link toxiccleanup.builder.entities.GameEntity}, the base class that all in-game objects
 * (tiles, machines, the player) extend, and {@link toxiccleanup.builder.entities.PlayerOverHook}, the
 * interface implemented by any entity that reacts when the player occupies the same grid cell.
 */
package toxiccleanup.builder.entities;
