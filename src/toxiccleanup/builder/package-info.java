/**
 * The top-level package for the Toxic Cleanup game. Contains the main entry point
 * ({@link toxiccleanup.builder.ToxicCleanup}), the shared {@link toxiccleanup.builder.GameState} interface passed to
 * every tickable component, the {@link toxiccleanup.builder.Tickable} interface implemented by all
 * game objects that update per frame, and the {@link toxiccleanup.builder.SpriteGallery} which holds
 * all sprite assets used throughout the game.
 */
package toxiccleanup.builder;
