/**
 * Root package for the Toxic Cleanup game. Contains the {@link toxiccleanup.Main} entry point,
 * which initialises the engine and runs the game loop. The game
 * logic lives under {@link toxiccleanup.builder} and the underlying rendering and input systems
 * live in the engine.
 */
package toxiccleanup;